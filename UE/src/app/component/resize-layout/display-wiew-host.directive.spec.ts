import { DisplayWiewHostDirective } from './display-wiew-host.directive';

describe('DisplayWiewHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayWiewHostDirective();
    expect(directive).toBeTruthy();
  });
});
